# Session: 2026-04-13 14:00

<!-- summary-only -->

## 主题
框架修复完成 + 两层记忆系统部署验证

## 关键决策
1. 3104 classifier 修好（numpy序列化 + workspace Apr 13新版），端口统一3104
2. 3103 scorer 修好（text字段兼容）
3. openclaw-router 判定半成品，disable systemd service
4. cron 重新配置：wiki-auto-learn每小时，memory-keeper每天4点，memory-maintain每天3点
5. 两层记忆系统部署：hook实时捕获 → ephemeral/ + session-summary LLM提炼
6. self-improving hook 改写：bootstrap时注入MEMORY.md + 最近3个摘要
7. gateway 重启完成，hook 实时记忆验证通过

## 重要发现
1. bootstrap 机制从未验证：AGENTS.md里的指令只是写在文件里，系统没有自动执行
2. 修复方法：改 self-improving hook，往 bootstrapFiles push MEMORY.md + summaries
3. ephemeral 触发条件：keyword(记住/决定用等) OR intercepted OR fault_recovery OR risk>=3 OR MEGA_TASK
4. 实时记忆验证通过（"记住我用的是 webchat" → ephemeral写入成功）

## 待跟进
- [ ] session-summary 在 /new 时跑通（ephemeral → entity升格）
- [ ] bootstrap 注入在下个 session 验证

<!-- source: agent:main:main -->

---

## 补充：/new 后操作步骤（已写入 MEMORY.md）

在 MEMORY.md 末尾新增了完整链路说明，包括：
1. 读取历史文件
2. 提炼 ephemeral
3. 验证记忆闭环
4. 确认框架状态

同时更新了 SOUL.md 启动检查规则，强制首次回复前读取 MEMORY.md。

## 已知未验证项
- session-summary hook
- bootstrap 注入
- ephemeral → entity 升格
